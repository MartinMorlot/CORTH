file_to_analyze <- file_loc

where_to_plot <- plot_loc

variable <- variable

col_to_rm <- T

anomaly <- T

name <- best_sel[1]

distance <- distance
FUNcluster <- hcut
k.max <- (nrow(df) - 1)
linecolor <- "steelblue"
hc_func <- "agnes"
hc_method <- "ward"

data <- select_cluster
