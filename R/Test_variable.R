variable <- variable <- "start"

file_to_analyze <- file_loc

where_to_plot <- plot_loc

col_to_rm <- T

anomaly <- T

name <- best_sel[1]

distance <- distance
FUNcluster <- hcut
k.max <- (nrow(df) - 1)
linecolor <- "steelblue"
hc_func <- "agnes"
hc_method <- "ward"

number_of_clusters <- 5

cluster_number <- 3

correction_loc <- corr_data_location <- correction_data_loc <- "/home/mmorlot/dev-work/CORTH/Data_correction_databases/"


data <- select_cluster

stations_name <- station_names
